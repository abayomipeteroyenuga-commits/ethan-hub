# Ethan Hub v2.3 — Approval Recognition Fix
The protected `ethan_profiles` record is authoritative.
Canonical roles include `super_admin` and `business_owner`.
The Hub now distinguishes pending approval from profile/RLS lookup errors.
The v2.2 Super Admin approval dashboard is preserved.
